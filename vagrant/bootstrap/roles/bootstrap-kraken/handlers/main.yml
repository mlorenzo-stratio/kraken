- name: Start kraken-agent service
  systemd:
    daemon_reload: yes
    name: kraken-agent
    enabled: yes
    state: restarted

- name: Start redis service
  systemd:
    daemon_reload: yes
    name: redis
    enabled: yes
    state: restarted