---
# - hosts: localhost
#   tasks:
#     - name: Build kraken
#       import_role:
#         name: bootstrap-kraken
#         tasks_from: build-kraken

- hosts: agents
  become: true
  tasks:
    - name: Import agent tasks from bootstrap-kraken role
      import_role:
        name: bootstrap-kraken
        tasks_from: agent

# - hosts: redis
#   become: true
#   tasks:
#     - name: Import redis tasks from bootstrap-kraken role
#       import_role:
#         name: bootstrap-kraken
#         tasks_from: redis

- hosts: origin
  become: true
  tasks:
    - name: Import origin tasks from bootstrap-kraken role
      import_role:
        name: bootstrap-kraken
        tasks_from: origin

- hosts: tracker
  become: true
  tasks:
    - name: Import tracker tasks from bootstrap-kraken role
      import_role:
        name: bootstrap-kraken
        tasks_from: tracker

- hosts: build_index
  become: true
  tasks:
    - name: Import build-index tasks from bootstrap-kraken role
      import_role:
        name: bootstrap-kraken
        tasks_from: build-index